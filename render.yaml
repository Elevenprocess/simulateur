services:
  - type: web
    name: sun-calc-reunion
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run preview --host
    publishDir: ./dist
    envVars:
      - key: NODE_ENV
        value: production